using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GlobalSettings
{
    public static int mouseSens = 100; // Start is called before the first frame update
   

    // Update is called once per frame
    
}
